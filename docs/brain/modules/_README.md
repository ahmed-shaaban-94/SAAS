# Modules

Notes about each functional module in the DataPulse codebase.

## Planned Contents

One file per module under `src/datapulse/`:
analytics, pipeline, bronze, billing, rbac, forecasting, anomalies,
gamification, embed, explore, lineage, notifications_center, onboarding,
reports, reseller, scenarios, targets, tasks, upload, views, watcher, ai_light.

Frontend modules: components, hooks, pages, lib.

Session notes already generate `[[wikilinks]]` to module names.
The graph will auto-connect once these files exist.
