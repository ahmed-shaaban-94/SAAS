# Second brain usage — pointer

Full guide in `docs/CONVENTIONS/second-brain.md`.

- **Session start**: read `docs/brain/_INDEX.md`.
- **After non-trivial bug fix**: write `docs/brain/incidents/YYYY-MM-DD-<slug>.md`.
- **After session-level decision**: write `docs/brain/decisions/YYYY-MM-DD-<slug>.md`.
- Session-end capture is automatic via `.claude/hooks/brain-session-end.sh`.
