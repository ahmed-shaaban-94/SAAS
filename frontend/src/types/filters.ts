export interface FilterParams {
  start_date?: string; // "YYYY-MM-DD"
  end_date?: string; // "YYYY-MM-DD"
  category?: string;
  brand?: string;
  site_key?: number;
  staff_key?: number;
  limit?: number;
}
