// Thin wrapper that re-exports usePosCart from the context.
// Keeps the hooks/ directory consistent — components import from hooks/, not contexts/.

export { usePosCart } from "@/contexts/pos-cart-context";
